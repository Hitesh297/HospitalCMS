@model IEnumerable<HospitalCMS.Models.DoctorDto>

@{
    ViewBag.Title = "List";
}

<h2>Doctor List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            Experience
        </th>
        <th>
            Phone
        </th>
        <th>
            Email
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.Name
        </td>
        <td>
            @item.Experience
        </td>
        <td>
            @item.Phone
        </td>
        <td>
            @item.Email
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DoctorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DoctorId }) |
            @Html.ActionLink("Delete", "ConfirmDelete", new { id=item.DoctorId })
        </td>
    </tr>
}

</table>
