@model  IEnumerable<HospitalCMS.Models.EventDto>


@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>Volunteer</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
<div class="form-group">
    <label class="control-label col-md-2">Volunteer :</label>
    <div class="col-md-10">
        <input class="form-control" style="max-width:100% !important" name="VolunteerName">
    </div>
</div>

    @*<div class="form-group">
        @Html.LabelFor(model => model.EventId, "EventId", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownList("EventId", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.EventId, "", new { @class = "text-danger" })
        </div>
    </div>*@

    <div class="form-group">
        <label class="control-label col-md-2">Event:</label>

        <div class="col-md-10">
            <select class="form-control" name="EventId">
                <option value="x">Please Select</option>
                @foreach (var event12 in Model as IEnumerable<HospitalCMS.Models.EventDto>)
                {
                    <option value="@event12.EventId">@event12.Title</option>

                }
            </select>
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
