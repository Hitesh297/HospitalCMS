@model HospitalCMS.Models.EventDto

@{
    ViewBag.Title = "ConfirmDelete";
}

<h2>ConfirmDelete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Event</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Title
        </dt>

        <dd>
            @Model.Title
        </dd>

        <dt>
            Date
        </dt>

        <dd>
            @Model.Date
        </dd>

        <dt>
            Picture
        </dt>

        <dd>
            @Model.HasPic
        </dd>

        <dt>
            Picture Extension
        </dt>

        <dd>
            @Model.PicExtension
        </dd>

        <dt>
            Description
        </dt>

        <dd>
            @Model.Description
        </dd>

        <dt>
            Department Id
        </dt>

        <dd>
            @Model.DepartmentId
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "Event", new { id = Model.EventId }, FormMethod.Post)) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "List")
        </div>
    }
</div>
